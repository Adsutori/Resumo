FIX:
- podglad classic - sidebar po lewej nie jest do konca strony, ale renderuje sie dobrze
- podglad minimal - po wyborze dwoch kolumn widac still jedną, ale renderuje sie dobrze


🔗 UDOSTĘPNIANIE CV

[ ] Dodać pole share_token do modelu CV (UUID)
share_token = models.UUIDField(default=uuid.uuid4, unique=True)


[ ] Widok publiczny CV: cv/share/<token>/ 
[ ] Przycisk Udostępnij generuje link i kopiuje do schowka (JS)
[ ] Opcja włącz/wyłącz udostępnianie dla każdego cv osobno - jeśli jest wyłączone to tylko user który je stworzył może je zobaczyć



📊 STATYSTYKI

[ ] Licznik pobrań PDF — pole download_count w modelu CV
[ ] Licznik wyświetleń — inkrementować przy każdym wejściu na publiczny link
[ ] Ukończenie profilu — obliczyć na podstawie wypełnionych pól usera + CV
[ ] Dni aktywności — policzyć z last_login lub osobnej tabeli aktywności


🔔 POWIADOMIENIA

[ ] Model Notification lub prostsze — Django messages framework
[ ] Czerwona kropka znika po przeczytaniu
[ ] Powiadomienia: pierwsze logowanie, ukończenie profilu, nowe funkcje


🔧 USTAWIENIA KONTA

[ ] Strona settings.html — zmiana nicku, emaila, hasła
[ ] Upload avatara użytkownika
[ ] Usunięcie konta z potwierdzeniem


🔜 WKRÓTCE (placeholder na teraz)

[ ] Analiza CV przez AI — badge "Wkrótce", docelowo integracja z OpenAI API
[ ] Śledzenie aplikacji — tabela z aplikacjami o pracę (firma, stanowisko, status)
[ ] Import z LinkedIn — wymaga LinkedIn API lub parsowania PDF eksportu


✅ REGULAMIN I POLITYKA PRYWATNOŚCI

[ ] Dodać podstrony


🧹 PORZĄDKI TECHNICZNE

[ ] LOGIN_REDIRECT_URL zmienić z /users/dashboard/ na /dashboard/
[ ] Dodać @login_required do wszystkich widoków dashboardu
[ ] favicon.ico — dodać do static/ (teraz 404)
[ ] Obsługa błędów 404 / 500 — własne strony błędów
[ ] Zmienne środowiskowe — upewnić się że SECRET_KEY i dane DB są w .env









Jesteś doświadczonym Django developerem. Zaimplementuj funkcję udostępniania CV w istniejącej aplikacji Django. Poniżej masz pełny kontekst i wymagania.

ISTNIEJĄCY MODEL CV
class CV(models.Model):
    user = models.ForeignKey(User, on_delete=models.CASCADE)
    title = models.CharField(max_length=200)
    template = models.CharField(max_length=50, default='classic')
    content = models.JSONField(default=dict, blank=True)
    design = models.JSONField(default=dict, blank=True)
    progress = models.IntegerField(default=0)
    is_active = models.BooleanField(default=True)
    download_count = models.IntegerField(default=0)
    view_count = models.IntegerField(default=0)
    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)
    # share_token i is_shared już istnieją lub trzeba dodać

ISTNIEJĄCE URLS (namespace: dashboard)
path('cv/<int:cv_id>/edit/',     views.edit_cv,      name='edit_cv'),
path('cv/<int:cv_id>/download/', views.download_pdf,  name='download_pdf'),
path('cv/<int:cv_id>/delete/',   views.delete_cv,     name='delete_cv'),
path('cv/<int:cv_id>/duplicate/',views.duplicate_cv,  name='duplicate_cv'),


CO MASZ ZAIMPLEMENTOWAĆ
1. Model (models.py)
Dodaj do modelu CV:
share_token = models.UUIDField(default=uuid.uuid4, unique=True)
is_shared = models.BooleanField(default=False)

Podaj też komendę migracji.
2. URL publiczny
cv/share/<uuid:token>/


Dostępny bez logowania
Jeśli is_shared=False → zwróć 404
Jeśli is_shared=True → renderuj CV jako HTML (użyj istniejących templatek PDF: dashboard/pdf/cv-classic.html itd.)
Inkrementuj view_count przy każdym wejściu

3. Widok toggle (POST)
cv/<int:cv_id>/toggle-share/


Wymaga logowania, tylko właściciel
Przełącza is_shared (True/False)
Zwraca JsonResponse z { "is_shared": true/false, "share_url": "..." }

4. Przycisk w dashboard.html
W każdej karcie CV (cv-card__actions) dodaj przycisk:
<button class="btn btn--ghost btn--sm" onclick="toggleShare({{ cv.id }}, '{{ cv.share_token }}', {{ cv.is_shared|yesno:'true,false' }})">
  <i data-lucide="share-2"></i>
  Udostępnij
</button>

5. JavaScript (dashboard.html — blok extra_js)
Funkcja toggleShare(cvId, token, isShared):

Wysyła POST na /dashboard/cv/<id>/toggle-share/ z CSRF tokenem
Jeśli is_shared staje się true → kopiuje link do schowka (navigator.clipboard) i pokazuje toast "Link skopiowany!"
Jeśli is_shared staje się false → pokazuje toast "Udostępnianie wyłączone"
Aktualizuje ikonę/tekst przycisku bez przeładowania strony


WYMAGANIA TECHNICZNE

Widok publiczny CV ma wyglądać identycznie jak PDF (użyj tych samych templatek), ale bez przycisku pobierania
Toast notification: prosty div wstrzykiwany do DOM, znika po 3 sekundach
Styl toastu zgodny z istniejącym systemem (dark theme, var(--color-accent), var(--color-surface))
Nie używaj żadnych zewnętrznych bibliotek JS


